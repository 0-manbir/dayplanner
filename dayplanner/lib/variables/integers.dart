// TASK VIEW SETTINGS---------------------------------------------------------

// number of lines in which a task is shown
const int maxLinesTaskView = 1;

// how long should a task be hovered before it is shown as a tooltip
const Duration taskHoverDuration = Duration(seconds: 2);

// how long should the 'new task textfield' be hovered before it is shown as a tooltip
const Duration newTaskTooltipHoverDuration = Duration(seconds: 2);
